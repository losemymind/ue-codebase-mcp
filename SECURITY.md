# Security policy

This system is designed to fail closed. Do not report secrets, private source, credentials, or production repository URLs in public issues. Use the organization's private security channel.

The MCP product boundary permanently excludes code writes, patch application, commits, pushes, submits, arbitrary commands, and arbitrary filesystem writes. Execution features, when introduced in an approved later phase, accept only administrator-defined preset identifiers.

