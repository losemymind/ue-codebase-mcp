# Phase 1 architecture (C4)

Status: implementation baseline complete; architecture and security review sign-off pending.

This document describes only Phase 1. It does not authorize Phase 2 work or relax any G1 gate. The system indexes an Unreal Engine 5.6 private Engine Fork and game C++/module sources from SVN, then exposes evidence-backed retrieval over Streamable HTTP MCP.

## Context

```mermaid
C4Context
  title UE Codebase MCP - system context
  Person(user, "Authorized developer", "Queries indexed source through an MCP client")
  Person(admin, "Administrator / operator", "Configures projects, ACLs, providers and approved jobs")
  System(mcp, "UE Codebase MCP", "Indexes UE 5.6 C++/module sources and returns bounded, revision-pinned evidence")
  System_Ext(client, "MCP client", "Codex Desktop/CLI/IDE, Claude Code, Cursor or OpenCode")
  System_Ext(svn, "Private SVN", "Source, revision history and authoritative path access")
  System_Ext(oidc, "Corporate OIDC", "User identity and signed JWTs")
  System_Ext(provider, "Approved embedding/rerank provider", "Administrator-configured OpenAI-compatible service")
  System_Ext(ops, "Operations systems", "TLS proxy, secret store, object storage and observability")

  Rel(user, client, "Uses")
  Rel(client, mcp, "Streamable HTTP MCP over internal TLS", "JSON-RPC")
  Rel(admin, mcp, "Administers through authenticated, audited interfaces")
  Rel(mcp, svn, "Reads source, revisions and ACLs")
  Rel(mcp, oidc, "Validates issuer, audience and signature")
  Rel(mcp, provider, "Sends policy-approved bounded batches")
  Rel(mcp, ops, "Uses protected platform services")
```

The MCP product never exposes code or arbitrary file writes, patch application, commit, push, submit, or a general-purpose shell. Phase 1 public tools are retrieval/status operations only. Later approved job tools may select an administrator-defined reindex, UBT or UAT preset; they still cannot accept command strings or mutate version-controlled source.

## Containers

```mermaid
C4Container
  title UE Codebase MCP - Phase 1 containers
  Person(user, "Authorized user")
  System_Ext(client, "MCP client")
  System_Ext(svn, "Private SVN")
  System_Ext(oidc, "Corporate OIDC")
  System_Ext(provider, "Embedding/rerank provider")

  System_Boundary(system, "UE Codebase MCP") {
    Container(proxy, "Reverse proxy", "TLS proxy", "Terminates internal TLS, limits request size/rate, applies security headers")
    Container(api, "MCP/API control plane", "TypeScript, Node.js 24.18.0", "Authenticates, authorizes, validates tools, orchestrates retrieval and records audit events")
    Container(retrieval, "Retrieval service", "TypeScript", "Combines exact match, PostgreSQL FTS, pgvector and graph evidence")
    Container(coordinator, "Index coordinator", "TypeScript", "Creates pinned revision sets, durable jobs and atomic index generations")
    Container(agent, "Windows Native Agent", "Windows Service, C++20/tool adapters", "SVN sync, compile database and UE 5.6-compatible Clang indexing")
    ContainerDb(db, "PostgreSQL", "PostgreSQL + pgvector + FTS", "Identity, ACL, metadata, symbols, graph, chunks, embeddings, jobs and audit")
    ContainerDb(store, "Protected object/file storage", "Internal storage", "Generation manifests, bounded logs, artifacts and backups")
    Container(obs, "Observability", "OpenTelemetry / Prometheus / Grafana", "Redacted metrics and traces")
  }

  Rel(user, client, "Uses")
  Rel(client, proxy, "Streamable HTTP MCP", "TLS")
  Rel(proxy, api, "Forwards validated network requests")
  Rel(api, oidc, "Validates JWT/JWKS")
  Rel(api, db, "Reads identity/ACL; writes audit and job metadata", "TLS")
  Rel(api, retrieval, "Requests authorized retrieval")
  Rel(retrieval, db, "Queries active authorized generation")
  Rel(retrieval, provider, "Optional bounded embedding/rerank requests", "TLS")
  Rel(coordinator, db, "Claims/leases jobs and publishes generations transactionally")
  Rel(agent, coordinator, "Claims jobs, heartbeats, submits signed/hash-bound manifests", "Mutual authenticated internal HTTP")
  Rel(agent, svn, "Read-only checkout/update/log/info/diff and ACL snapshot")
  Rel(agent, store, "Writes manifests and redacted bounded artifacts")
  Rel(api, obs, "Emits redacted telemetry")
  Rel(coordinator, obs, "Emits redacted telemetry")
  Rel(agent, obs, "Emits redacted telemetry")
```

No client can connect directly to PostgreSQL, object storage, an Agent internal endpoint, an SVN workspace, or observability administration. Agent endpoints bind only to the management network or localhost and use short-lived Agent credentials distinct from user MCP tokens.

## Control-plane components

```mermaid
C4Component
  title MCP/API control plane components
  Container_Boundary(api, "MCP/API control plane") {
    Component(transport, "Streamable HTTP MCP transport", "Protocol adapter", "Initialize, list and call with request/response bounds")
    Component(authn, "Authentication", "OIDC/JWT and hashed bearer tokens", "Rejects anonymous, invalid, expired or revoked identities")
    Component(authz, "Authorization policy engine", "SVN snapshot intersection MCP ACL", "Computes project/team/user and path visibility; fails closed")
    Component(schema, "Tool schema registry", "JSON Schema", "Allows only named, typed tools; no command or path escape hatch")
    Component(orchestrator, "Retrieval orchestrator", "Application service", "Pins active generation and requests authorized retrieval")
    Component(evidence, "Evidence packager", "Application service", "Bounds snippets and attaches path, lines, SVN revision, generation and uncertainty")
    Component(audit, "Audit writer", "Application service", "Records actor, action, scope, outcome and request hash without secrets/full code")
    Component(config, "Configuration loader", "Versioned YAML/schema", "Resolves secret references and fails startup on invalid configuration")
  }

  ContainerDb(db, "PostgreSQL")
  Container(retrieval, "Retrieval service")
  System_Ext(client, "MCP client")
  System_Ext(oidc, "OIDC / token authority")
  System_Ext(secrets, "Secret store")

  Rel(client, transport, "MCP request")
  Rel(transport, authn, "Authenticates")
  Rel(authn, oidc, "Validates or refreshes keys")
  Rel(authn, authz, "Authenticated principal")
  Rel(authz, db, "Loads current MCP grants and fresh SVN ACL snapshot")
  Rel(transport, schema, "Validates tool name/input")
  Rel(schema, orchestrator, "Typed authorized call")
  Rel(orchestrator, retrieval, "Generation-pinned query and allowed scope")
  Rel(retrieval, evidence, "Candidate evidence")
  Rel(evidence, transport, "Bounded response")
  Rel(transport, audit, "Outcome")
  Rel(audit, db, "Append audit event")
  Rel(config, secrets, "Resolves reference at runtime")
```

Authorization is applied before candidate retrieval and again while packaging evidence. The effective permission relation is:

```text
effective_access(principal, repository, path, revision)
  = current_mcp_acl(principal, project, repository, path)
  intersection fresh_svn_acl_snapshot(principal, repository, path, revision)
```

Missing, stale, invalid or indeterminate identity/ACL inputs produce no access. Search counts, timing details, symbol existence and path normalization errors must not become an authorization side channel.

## Index generation and query invariants

1. A generation is bound to an immutable revision set before work begins; an SVN HEAD change cannot move it.
2. Agent workspaces are read-only from the product perspective and isolated by project, repository, revision and job.
3. Index output first enters a non-queryable staging generation. Manifest hashes, revision mapping, ACL provenance and completeness checks must pass before one database transaction changes the active generation.
4. Queries see exactly one active generation. Failed publication retains the previous active generation.
5. Every source result includes project/repository identifiers, `repository_kind=svn`, branch, revision, generation, relative normalized path, line range, bounded snippet when authorized, score/match reasons, uncertainty and indexed time.
6. Provider failures may degrade semantic ranking to exact/FTS retrieval; they cannot bypass authorization or evidence requirements.
7. Correlation IDs join request, job, Agent and audit records without placing tokens, secrets or complete source payloads in telemetry.

## Phase and scale constraints

- Phase 1 VCS is SVN only. Git and Perforce require the post-G3 extension phase.
- UE tool integration targets UE 5.6 and the private Fork's real compile database; guessed global flags are forbidden.
- The initial data plane is PostgreSQL plus pgvector and FTS. A specialized search/vector service requires Phase 3 benchmark evidence and a separate ADR.
- The expected operating point is about 30 users and 10 concurrent queries, with query P95 at most 5 seconds and P99 at most 10 seconds.
- Code freshness is at most 5 minutes; initial Engine Fork plus first project indexing is at most 24 hours.

## Review evidence required

Architecture and security reviewers must verify the diagrams against deployed network policy, configuration schema, database roles, Agent service identity, ACL negative tests and the enumerated MCP schemas. This document does not claim those human reviews have occurred.
